{
  "name": "job-application-tracker",
  "version": "1.0.0",
  "description": "A comprehensive MERN stack application to track job applications",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run start:dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "start": "npm run start:server",
    "start:server": "cd server && npm run start:prod",
    "start:client": "cd client && npm run start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "clean": "rm -rf node_modules server/node_modules client/node_modules server/dist client/.next"
  },
  "keywords": [
    "job-tracker",
    "mern-stack",
    "nextjs",
    "nestjs",
    "mongodb",
    "typescript"
  ],
  "author": "Mizanur Rahman Ashiq",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
